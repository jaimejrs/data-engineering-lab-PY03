psycopg2-binary==2.9.9
SQLAlchemy>=1.4.36,<2.0
# <2.32 exigido por apache-airflow-providers-docker 3.12; api_extractor não usa
# nada específico de 2.32.
requests>=2.27,<2.32
pandas==2.1.4
pyarrow==15.0.2
hdfs==2.7.3
python-dotenv==1.0.1

# Cliente Spark para o SparkSubmitOperator (DAG 2 submete em client mode).
# O wheel do pyspark já traz spark-submit + os jars do Spark 3.5.3 — evita baixar
# o tarball do Spark. Versão casada com a imagem do cluster (docker/spark).
apache-airflow-providers-apache-spark==4.8.1
pyspark==3.5.3

# DAG 3 dispara o dbt (imagem datalab-dbt) via DockerOperator.
apache-airflow-providers-docker==3.12.0

# DAG 4 (ml_inference) — Modelo 1 (Isolation Forest) e Modelo 2 (XGBoost
# quantile) rodam direto na imagem do Airflow via PythonOperator, mesmo padrão
# da DAG 1 (import direto de src/extractors) — sem puxar um container extra só
# pra isso, ao contrário de Spark/dbt (esses sim pesados o bastante pra
# justificar DockerOperator). Ver models/anomaly_detection.py e
# models/payment_forecast.py.
scikit-learn==1.5.0
joblib==1.4.2
xgboost==2.0.3
trino==0.328.0
