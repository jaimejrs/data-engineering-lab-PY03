streamlit>=1.35
plotly>=5.20

# Versões fixas iguais ao requirements.txt da raiz do projeto — este painel lê
# as mesmas tabelas Iceberg que o resto do pipeline, então usar uma versão
# solta aqui arriscaria drift de comportamento (ex: parsing de DECIMAL do
# driver Trino, serialização do pandas) entre o painel e o pipeline principal.
trino==0.328.0
pandas==2.2.2
openai==1.51.0
# httpx<0.28: openai==1.51.0 repassa `proxies=` ao criar o httpx.Client interno;
# httpx>=0.28 removeu esse kwarg -> "Client.__init__() got an unexpected
# keyword argument 'proxies'" ao instanciar OpenAI() (erro visto na aba Resumo IA).
httpx<0.28
python-dotenv==1.0.1