# Copie este arquivo para ".env" (mesma pasta do app.py) e preencha os valores.
# O .env NUNCA deve ser commitado em um repositório (já coberto pelo .gitignore
# da raiz do projeto, regra `.env`, que se aplica em qualquer subpasta).

# --- Conexão com o Trino ---
TRINO_HOST=100.69.31.14
TRINO_PORT=8085
TRINO_USER=seu-nome
TRINO_CATALOG=iceberg

# --- OpenAI (aba "Resumo (IA)") ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
