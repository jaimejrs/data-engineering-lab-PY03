# Orquestração
apache-airflow[postgres,http]==2.9.3

# Conexão com PostgreSQL
psycopg2-binary==2.9.9
sqlalchemy>=1.4.36,<2.0  # apache-airflow==2.9.3 exige sqlalchemy<2.0

# Chamadas à API REST
requests==2.32.3

# Manipulação e serialização de dados
pandas==2.2.2
pyarrow==15.0.2  # mlflow==2.14.1 exige pyarrow<16

# Armazenamento distribuído (HDFS via WebHDFS)
hdfs==2.7.3

# Cliente Trino — consulta Silver/Gold (Iceberg) para EDA e notebooks
trino==0.328.0

# Machine Learning / IA
scikit-learn==1.5.0
joblib==1.4.2  # persistência dos modelos treinados (models/anomaly_detection.py)
xgboost==2.0.3
prophet==1.1.5
mlflow==2.14.1

# IA Generativa — relatório narrativo (models/narrative_report.py, tarefa 25)
openai==1.51.0
# httpx<0.28: openai==1.51.0 repassa `proxies=` ao criar o httpx.Client interno;
# httpx>=0.28 removeu esse kwarg -> "Client.__init__() got an unexpected
# keyword argument 'proxies'" ao instanciar OpenAI().
httpx<0.28

# Validação de qualidade de dados (opcional)
great_expectations==0.18.19

# Configuração e testes
python-dotenv==1.0.1
pytest==8.2.2
pyyaml==6.0.2  # tests/test_compose_sync.py — compara os dois docker-compose
ruff==0.6.9  # lint + format (ver pyproject.toml [tool.ruff]), único binário, sem config separada de formatter

# Notebooks
jupyter==1.0.0
ipykernel==6.29.4
